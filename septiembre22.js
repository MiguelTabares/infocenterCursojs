// EVENTOS EN JS

// Los eventos son acciones que puede ejecutar el usuario.

// Un prompt es un input o también un mensaje que consiste en un pop-up o recuadro emergente
function promptMe(){
    var entradaUsuario = prompt("Ingrese por favor su nombre: ");
    alert (entradaUsuario);
}